Temporal Dead Zone (TDZ):- 
 is a concept in JavaScript where variables declared with let and const are not accessible before the line of code where they are declared.
 Dead Zone is the time between the variable declaration and the initial assignment.